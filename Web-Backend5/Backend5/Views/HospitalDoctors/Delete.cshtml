@model Backend5.Models.HospitalDoctor

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>HospitalDoctor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Doctor)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Doctor.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Hospital)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Hospital.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-route-hospitalId="@Model.HospitalId" asp-route-doctorId="@Model.DoctorId">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index" asp-route-hospitalId="@Model.HospitalId">Back to List</a>
        </div>
    </form>
</div>
