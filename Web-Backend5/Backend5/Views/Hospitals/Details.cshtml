@model Backend5.Models.Hospital

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Hospital</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Address)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Phones)
        </dt>
        <dd>
            @foreach (var phone in Model.Phones)
            {
                @phone.Number<br/>
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index" asp-controller="Wards" asp-route-hospitalId="@Model.Id">Wards</a> |
    <a asp-action="Index" asp-controller="HospitalLabs" asp-route-hospitalId="@Model.Id">Labs</a> |
    <a asp-action="Index" asp-controller="HospitalDoctors" asp-route-hospitalId="@Model.Id">Doctors</a> |
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
